apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: habesha-table
spec:
  selector:
    app: backend
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
