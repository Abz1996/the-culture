apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: habesha-table
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
